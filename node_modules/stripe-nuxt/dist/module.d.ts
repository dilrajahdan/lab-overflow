/// <reference types="@types/stripe-v3" />
import { Module } from '@nuxt/types';
declare module 'vue/types/vue' {
    interface Vue {
        $stripe: stripe.Stripe;
    }
}
declare global {
    interface Window {
        Stripe: stripe.StripeStatic;
    }
}
declare type Options = string;
declare const stripeModule: Module<Options>;
export declare const meta: any;
export default stripeModule;
