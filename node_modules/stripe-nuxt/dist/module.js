import path from 'path';
const stripeModule = function (publishableKey) {
    if (typeof publishableKey !== 'string' || !publishableKey.length) {
        throw new Error('Please provide a publishable key to the Stripe module');
    }
    this.options.head.script.push({
        src: `//js.stripe.com/v3/`,
        async: true,
        defer: true,
    });
    this.addPlugin({
        src: path.resolve(__dirname, './templates/plugin.js'),
        ssr: false,
        options: {
            publishableKey
        }
    });
};
export const meta = require('../package.json');
export default stripeModule;
//# sourceMappingURL=module.js.map